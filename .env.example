# Server Configuration
HOST=0.0.0.0
PORT=8000

# TTS Configuration
MAX_CHUNK_CHARS=320
CHUNK_GAP_MS=120

# Device: auto, cuda, mps, or cpu
DEVICE=auto

# Default voice name (must exist in voices/ directory)
DEFAULT_VOICE=dan

# Output format: mp3 or wav
DEFAULT_OUTPUT_FORMAT=mp3
